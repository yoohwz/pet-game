# Testing

Run `godot --headless --path . -s res://tests/test_runner.gd`. Tests retain Phase 0–6 coverage and add Phase 7 English-only rules, complete-token/phrase boundary matching, tab/LF/CR normalization, priority/question/OTHER fallbacks, input-state rejection, canonical `pet_heard_message` mapping validation, honest Memory v2 runtime config, active+memorial migration, hard repeat-window/familiar-topic behavior, bounded raw language eviction with durable summaries, candidate-failure authority, memorial preservation/replacement isolation, and stable UI availability/diagnostics. A pass exits 0; any failure exits non-zero.

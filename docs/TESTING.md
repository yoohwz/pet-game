# Testing

Run `godot --headless --path . -s res://tests/test_runner.gd`. Tests retain Phase 0–2 coverage and add executable Phase 3 v3→v4 egg/HATCHING/pet migration, activity/vital validation, care clamps/rejections and candidate failures, offline sleep, complete sleep values/clamping/chunking, supplied-care event semantics, interaction timeline ordering, stable awake/sleep controls, and reaction feedback. A pass exits 0; any failure exits non-zero.

# Testing

Run `godot --headless --path . -s res://tests/test_runner.gd`. Tests retain Phase 0–7 coverage and add Phase 8 checks for the `Pet Game` 360×640 nearest-neighbor project shell, hidden-by-default developer panel, read-only player/developer refresh, reachable debug Time Machine controls, companion state projection, six readable needs, lifecycle control matrix, mobile-sized action targets, non-verbal English feedback, and control stability across value/reaction/language refreshes. A pass exits 0; any failure exits non-zero.

# Testing

Run `godot --headless --path . -s res://tests/test_runner.gd`. Tests retain Phase 0–3 coverage and add Phase 4 schema-v5 migration/validation (including v4 egg/HATCHING), newborn protection, zero-need boundary timing, survival-version semantics, deterministic CRITICAL/death chronology and chunking, rescue/partial rescue, permanent death via resume/debug/backward clock, dead-care rejection, memorial candidate failure authority and exact-once behavior (including legacy count preservation), replacement New Egg candidate failure authority, explicit 4-hour replacement incubation and offline READY/no-auto-hatch, and stable DEAD/MEMORIAL/CRITICAL-sleeping presentation. A pass exits 0; any failure exits non-zero.

# Testing

Run `godot --headless --path . -s res://tests/test_runner.gd`. Tests retain Phase 0–5 coverage and add Phase 6 canonical schema-v1 `pet_grew` event validity, real growth-config loading, schema-v7/new-pet and strict growth validation, exact 2d/7d/21d boundaries, offline multi-stage and Application-memory chunking/exact-once equivalence, cross-cycle memorial/replacement isolation, Application dead freeze, exact-whole/fractional growth persistence handling, complete v6 egg/HATCHING field migration, deterministic runtime growth-version/event identity, lifecycle-memory care-summary independence, sleeping/CRITICAL growth, death cutoff/same-second ordering, backward no-regression, and stable stage UI controls/inspector data. A pass exits 0; any failure exits non-zero.

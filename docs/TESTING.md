# Testing

Run `godot --headless --path . -s res://tests/test_runner.gd`. Tests retain Phase 0–7 coverage and add Phase 8 checks for the `Pet Game` 360×640 nearest-neighbor project shell, hidden-by-default developer panel, read-only player/developer refresh, deterministic UTC memorial formatting/fallback, scroll-hierarchy reachability, mobile-sized action targets, companion state projection, six readable needs, full lifecycle control matrix, complete care-feedback callbacks, non-verbal English feedback, and control stability across value/reaction/language refreshes. The real Developer Time Machine Buttons are exercised for egg readiness, needs, growth, CRITICAL and death production paths. A pass exits 0; any failure exits non-zero.

# Testing

Run `godot --headless --path . -s res://tests/test_runner.gd`. Tests retain Phase 0–4 coverage and add Phase 5 schema-v6 active/nested-memorial migration, strict `memory_version` and exact-whole JSON canonicalization/round-trip validation, relationship rewards/cooldown/version honesty, actual post-clamp base/rescue deltas, candidate-failure atomicity (including failed rescue), deterministic survival-memory chunking, memory mappings and pure views, idempotent projection, routine non-duplication for stabilization, bounded priority eviction including equal-priority ordering, hatch/CRITICAL/death/rescue chronology, dead-memory freeze, and read-only inspector stability. A pass exits 0; any failure exits non-zero.

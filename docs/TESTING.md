# Testing

Run `godot --headless --path . -s res://tests/test_runner.gd`. Tests retain Phase 0–4 coverage and add Phase 5 schema-v6 active/nested-memorial migration, relationship rewards/cooldown/version honesty, candidate-failure atomicity, memory mappings and pure views, idempotent projection, bounded eviction, hatch/CRITICAL/death/rescue chronology, dead-memory freeze, and read-only inspector stability. A pass exits 0; any failure exits non-zero.

# Testing

Run `godot --headless --path . -s res://tests/test_runner.gd`. Tests retain Phase 0–3 coverage and add Phase 4 schema-v5 migration/validation, newborn protection, zero-need boundary timing, survival-version semantics, deterministic CRITICAL/death transitions and chunking, rescue/partial rescue, permanent death, dead-care rejection, atomic memorialization (including legacy count preservation), explicit replacement-egg persistence, and DEAD/MEMORIAL stable presentation. A pass exits 0; any failure exits non-zero.

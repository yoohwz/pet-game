# Testing

Run `godot --headless --path . -s res://tests/test_runner.gd`. Tests retain Phase 0–5 coverage and add Phase 6 real growth-config loading, schema-v7/new-pet and strict growth validation, exact 2d/7d/21d boundaries, offline multi-stage and chunking equivalence, deterministic runtime growth-version/event identity, lifecycle-memory exact-once/order and care-summary independence, sleeping/CRITICAL growth, death cutoff/same-second ordering/dead freeze, v6 active/dead/memorial/egg/HATCHING migration including zero-elapsed catch-up, backward no-regression, and stable stage UI controls/inspector data. A pass exits 0; any failure exits non-zero.
